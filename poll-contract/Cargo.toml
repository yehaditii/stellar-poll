[package]
name = "poll-contract"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
soroban-sdk = { version = "21.7.7", default-features = false }

[features]
default = []

[profile.release]
opt-level = "z"
overflow-checks = true
debug = false
strip = "symbols"